/// <reference types="node" />
import { ChildProcess, SpawnOptions } from 'child_process';
interface SpawnPromise<T> extends Promise<T> {
    child: ChildProcess;
}
interface SpawnResult {
    pid: number;
    output: string[];
    stdout: string;
    stderr: string;
    status: number | null;
    signal: string | null;
}
declare const _default: (command: string, args?: ReadonlyArray<string> | undefined, options?: SpawnOptions | undefined) => SpawnPromise<SpawnResult>;
export = _default;
